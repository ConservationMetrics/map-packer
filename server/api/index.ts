// import { checkAuthStrategy } from "../middleware/middleware";
// import { getLogin, postLogin } from "../middleware/loginController";

// app.get("/login", getLogin);
// app.post("/login", postLogin);

// Apply middleware to routes
// app.use(checkAuthStrategy);
